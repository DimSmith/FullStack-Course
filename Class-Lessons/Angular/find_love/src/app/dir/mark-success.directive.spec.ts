import { MarkSuccessDirective } from './mark-success.directive';

describe('MarkSuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkSuccessDirective();
    expect(directive).toBeTruthy();
  });
});

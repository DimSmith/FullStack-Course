import { MarkFailDirective } from './mark-fail.directive';

describe('MarkFailDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkFailDirective();
    expect(directive).toBeTruthy();
  });
});
